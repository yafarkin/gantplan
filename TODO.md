# TODO — бэклог по итогам аудита (2026-08)

После сессии по план/факт-логике (`Solver.cs`, `DemoProject.cs`, тесты)
прогнали аудит остального кода и гигиены репозитория. Ничего из этого пока
не реализовано — просто чтобы не потерять. Пункты 2, 4, 6 (ниже) перепроверены
чтением кода напрямую, остальные — по итогам агентского исследования (пути и
строки указаны, но не запускались вживую).

## Высокий приоритет — дёшево и реально может укусить

1. **Нет CI.** Ни `.github/workflows`, ни любого другого CI-конфига нет
   вообще. 33 теста сейчас гоняются только руками — ничего не мешает
   регрессии тихо попасть в `master`. 15 строк YAML (checkout → restore →
   build → test) закрывают это почти полностью.

2. **`Solve() == false` не прерывает `Program.cs`.** `Planner/Program.cs:29-33`:
   при неудаче печатается предупреждение, но дальше код всё равно
   сериализует `project.json`, генерирует `timeline.html` и открывает его —
   как будто всё хорошо. Никакого `Environment.Exit`/return. Скрипт,
   вызывающий этот бинарник и проверяющий код возврата, никогда не узнает о
   провале.

3. **`CalendarLogic` крашится на пересекающихся ГЛОБАЛЬНЫХ периодах, но не
   на персональных.** `GantSolver/Logic/CalendarLogic.cs:29,38` vs `:47,56`:
   для `globalCalendar` используется `.SingleOrDefault(...)` (бросит
   `InvalidOperationException`, если два периода в `NonWorkingDays`/
   `WorkingDays` перекрываются на одну дату), а для `resourceCalendar` —
   `.FirstOrDefault(...)` (спокойно берёт первый). Реалистичный сценарий:
   общий календарь праздников собран из двух источников (гос. праздники +
   компания), даты пересеклись — весь `Solve()` падает необработанным
   исключением на ровном месте.

4. **`StartAfterTest` содержит подавленный, не разобранный баг.**
   `Tests/SolverTests.cs:396`:
   ```csharp
   //Assert.That(tasks[1].Plan!.PlannedFinish, Is.EqualTo(new DateOnly(2026, 01, 9))); // fix later
   ```
   Кто-то уже наткнулся на расхождение между ожиданием и реальным
   результатом при взаимодействии `StartAfter` у одной задачи и несвязанной
   соседней на том же ресурсе — и просто закомментировал проверку вместо
   того, чтобы разобраться.

5. **`DueDate` vs уже начатая (`Fact.IsProgress`) задача — ноль тестов,
   ноль объяснения при провале.** Если задача уже в работе, её старт и
   длительность в модели становятся жёсткой константой
   (`Solver.cs:339-354`); если это делает недостижимым `DueDate`, модель
   становится infeasible, `Solve()` вернёт `false` неотличимо от любой
   другой причины провала.

6. **`ResourceDto.Percent` без валидации диапазона.**
   `Solver.cs:570-573`: `duration * 100.0 / resource.Percent`. При
   `Percent == 0` — `Convert.ToInt32(double.PositiveInfinity)` бросает
   необработанный `OverflowException`, роняя весь прогон с непонятным
   сообщением вместо человеческой ошибки валидации. Отрицательный `Percent`
   не падает, но тихо даёт бессмысленную 1-дневную длительность. Ни одного
   теста на `Percent` нет вообще.

## Средний приоритет — не срочно, но стоит знать, что дыра есть

7. **`Horizon = 180` дней захардкожен**, без единого сигнала при
   превышении. Проект, которому реально нужно больше полугода (например,
   старт под конец фискального года + XL-задачи), тихо получит
   `Solve() == false` неотличимо от настоящего конфликта расписания.

8. **Факт/план-цикл почти не задокументирован.** `README.md`/`explain.md`
   упоминают "перепланирование" одной фразой; ничего не сказано про то, что
   уже начатая задача жёстко фиксируется в модели, что "перепланирование" —
   это буквально повторный вызов `Solve()` на том же `ProjectDto`, и про
   guard от бэкдейтинга (`Solver.cs`, `CreateDependencyBetweenTasks`,
   `hasAlreadyCompletedWork`). Всё это сейчас узнаваемо только чтением
   `Solver.cs`.

9. **`TaskFactRecordType.ChangedAssignee` — по факту мёртвый код.**
   Встречается только в объявлении enum; нигде не обрабатывается отдельно
   от любой другой записи с `ResourceName`. Работает "случайно" через общий
   `Records.Last(x => x.ResourceName != null)`, а не потому, что для него
   есть своя логика. Либо дать этому типу реальный смысл (явный сброс
   оставшейся длительности при смене исполнителя), либо явно
   задокументировать как неиспользуемый.

## Низкий приоритет / чисто гигиена — по желанию

10. Нет `.editorconfig`, нет `TreatWarningsAsErrors`/`Directory.Build.props`
    (только `Nullable enable`, дальше ничего не форсируется).
11. `SolverTests.cs` (835 строк) и `TaskAlignmentTests.cs` (684 строки) уже
    большие, без `[Category]` — не срочно, но при дальнейшем росте стоит
    разбить по темам.
12. Остатки закомментированного кода/альтернативных реализаций в
    `Solver.cs` (строки 64, 82-94, 349) — не мешают, но можно убрать заодно.
</content>
